char *ft_strrchr(const char *s, int c)
{
    int i;
    int pos;

    i = 0;
    pos = 0;
    while (s[i])
	{
		if (s[i] == (char)c)
            pos = i;
		i++;
	}
    if(pos != 0)
    {
        *s += pos;
	    return (*s);
    }
    return (0);
}